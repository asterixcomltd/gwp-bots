# ════════════════════════════════════════════════════════════════════
# GWP ALTCOIN BOT v3.0 — GitHub Actions Workflow
# Pairs: DEXE · UNI · SUSHI · SOL · AVAX · BTC · ETH (KuCoin)
# State: altcoin_state.json (committed back to repo)
# © 2026 Asterix.COM Ltd. / Abdin
# ════════════════════════════════════════════════════════════════════

name: GWP Altcoin Bot v3.0

on:
  schedule:
    - cron: '15,45 * * * *'   # Main scan every 30 min (offset 15 from forex)
    - cron: '2 8 * * *'       # Daily summary 08:02 UTC
    - cron: '7 8 * * 1'       # Weekly summary Monday 08:07 UTC
  workflow_dispatch:
    inputs:
      mode:
        description: 'Run mode: scan / daily / weekly / health'
        required: false
        default: 'scan'

permissions:
  contents: write

jobs:
  run-altcoin-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Determine run mode
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "MODE=${{ github.event.inputs.mode }}" >> $GITHUB_ENV
            exit 0
          fi
          HOUR=$(date -u +%H)
          MIN=$(date -u +%M)
          DAY=$(date -u +%u)
          if [ "$DAY" = "1" ] && [ "$HOUR" = "08" ] && [ "$MIN" -ge "6" ] && [ "$MIN" -le "11" ]; then
            echo "MODE=weekly" >> $GITHUB_ENV
            exit 0
          fi
          if [ "$HOUR" = "08" ] && [ "$MIN" -lt "10" ]; then
            echo "MODE=daily" >> $GITHUB_ENV
            exit 0
          fi
          echo "MODE=scan" >> $GITHUB_ENV

      - name: Run GWP Altcoin Bot
        env:
          ALTCOIN_TG_TOKEN: ${{ secrets.ALTCOIN_TG_TOKEN }}
          ALTCOIN_CHAT_ID:  ${{ secrets.ALTCOIN_CHAT_ID }}
        run: node altcoin_bot.js $MODE

      - name: Save state to repo
        run: |
          git config user.email "gwp-altcoin@asterixcom.ltd"
          git config user.name "GWP Altcoin Bot"
          git add altcoin_state.json
          git diff --staged --quiet || git commit -m "state(altcoin): $(date -u +%Y-%m-%dT%H:%MZ) [skip ci]"
          git push || true